tii Baa.also(fn) {
    fn.weele(ale)
    segin niin ale
}
tii Baa.let(fn) {
    segin niin fn.weele(ale)
}
tii test(fn) {
    segin niin fn.weele("molo", 12)
}

A.yira([].also {it =>
    it.aBeeFaraAkan([12, 34, 44])
})
